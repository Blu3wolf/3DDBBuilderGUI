<Window x:Class="_3DDBBuilderGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3DDBBuilderGUI"
        xmlns:properties="clr-namespace:_3DDBBuilderGUI.Properties"
        mc:Ignorable="d"
        Title="F4-3DDBBuilder" Height="350" Width="525">
    <Window.Resources>
        <Style x:Key="WFButton" TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="100" />
            <Setter Property="Margin" Value="10,0,10,0" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>
        <Style TargetType="TextBox" >
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>
    </Window.Resources>
    <DockPanel>
        <Label x:Name="statuslabel" Content="Status Line (You shouldnt see this text though)" DockPanel.Dock="Bottom" />
        <TabControl >
            <TabItem Header="Extract">
                <Grid Background="#FFE5E5E5">
                    <StackPanel>
                        <Label Content="Specify location of Object folder"/>
                        <DockPanel>
                            <Button x:Name="SourceSelectButton" Content="Select..." Click="SourceSelectButton_Click" Style="{StaticResource WFButton}"/>
                            <ComboBox x:Name="DBBox" Margin="0,0,10,0"/>
                        </DockPanel>
                        <Label Content="Specify destination for extracted DB"/>
                        <DockPanel>
                            <Button x:Name="DestSelectButton" Content="Select..." Click="DestSelectButton_Click" Style="{StaticResource WFButton}" DockPanel.Dock="Right"/>
                            <TextBox x:Name="ExtractionTextBox" TextWrapping="Wrap" Text="{Binding Mode=TwoWay, Path=CurExtractionPath}" />
                        </DockPanel>
                        <Button Content="Extract" Click="ExtrButton_Click" Margin="10" Style="{StaticResource WFButton}"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="List Parents">
                <Grid Background="#FFE5E5E5">
                    <StackPanel>
                        <Label Content="Specify location of Object folder"/>
                        <DockPanel>
                            <Button x:Name="LPSourceSelectButton" Content="Select..." Click="LPSourceSelectButton_Click" Style="{StaticResource WFButton}"/>
                            <ComboBox x:Name="LPDBBox" ItemsSource="{Binding DBlist}" DisplayMemberPath="Dir" SelectedValuePath="Dir" Margin="0,0,10,0"/>
                        </DockPanel>
                        <Button Content="List Parents" Click="ListParents" Margin="10" Style="{StaticResource WFButton}"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Build" >
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Update" >
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Test DB" >
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
